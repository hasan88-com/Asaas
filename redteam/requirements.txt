# Red-Teaming Engine — standalone requirements.
# `--target direct` additionally needs backend/requirements.txt installed
# (it imports the app package directly).
pyyaml>=6.0
httpx>=0.28.1
pytest>=8.0
pytest-asyncio>=0.24
